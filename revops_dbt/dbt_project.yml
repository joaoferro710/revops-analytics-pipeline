name: 'revops_dbt'
version: '1.0.0'

profile: 'revops_dbt'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

models:
  revops_dbt:
    staging:
      +materialized: view
    marts:
      +materialized: table